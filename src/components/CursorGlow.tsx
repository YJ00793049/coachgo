// Retired for the "Editorial Calm" reskin — the warm/quiet aesthetic uses the
// native cursor. Kept as a no-op so any lingering imports stay valid.
export default function CursorGlow() {
  return null;
}
